# 2026-04-09

## Summary

- Scaffolded the first Cargo workspace for `ssw-rs`.
- Added `ssw-core`, `ssw-actix`, `ssw-html`, and `ssw-components`.
- Implemented a minimal compileable API surface and verified it with `cargo test`.

## Facts

- `ssw-core` currently defines `Render`, `HtmlKind`, `HtmlResponse`, `TextResponse`, `Redirect`, and `Response`.
- `ssw-html` currently uses a small owned `Markup` type with escaping helpers and simple document composition.
- `ssw-actix` currently exposes response conversion helpers rather than a more ambitious adapter abstraction.
- `ssw-components` currently contains only thin proof-of-shape helpers.

## Risks

- The current HTML layer is intentionally too small to judge long-term ergonomics.
- `Render` may prove too weak or too general once layouts, assets, and partial rendering become more concrete.
- No example app exists yet, so the current API has only unit-test pressure.

## Todo

- Build a minimal example or integration flow in Actix.
- Revisit whether `ssw-html` should stay owned, wrap Maud, or become a Maud-derived fork.
- Start shaping page, fragment, and redirect ergonomics around a real handler workflow.

## Additional work

- Added an initial `html!` macro with support for nested elements, string literals, escaped expressions, boolean and valued attributes, `@if`, and `@for`.
- Kept the macro implementation behind an internal `ssw-html-macros` crate while re-exporting the public API from `ssw-html`.
- Added tests that define the intended authoring style instead of optimizing only for low-level rendering helpers.

- Expanded `ssw-html` with empty-tag syntax, `#id` and `.class` shorthand, optional attribute omission via `Option<T>`, and a `Document` builder for page layouts.
- Added layout-shaped tests so `ssw-html` is being driven by real page authoring patterns rather than just raw string generation.
- Improved class composition so `.class` shorthand now merges with explicit `class=(...)` values, including optional and tuple-composed values.
- Current limitation: `#id` shorthand still does not compose with explicit `id=...` attributes.

- Added an end-to-end Actix test flow that serves a document page, an HTML fragment, and redirects using the current `ssw-html` API.
- Added thin `ssw-actix` convenience helpers (`page`, `fragment`, `redirect`) and `Responder` support for `ActixResponse` to make handler code read like the framework model.

- Fixed a real `ssw-html` semantic bug: expression-based boolean HTML attributes now render by presence or omission for standard boolean attrs, while non-boolean attrs still render string values.

- Added an end-to-end POST form flow in Actix with invalid redisplay, preserved input values, and redirect-after-success behavior.
- Confirmed that the current `ssw-html` surface is already usable for simple SSR form workflows without requiring a separate form abstraction yet.

- Expanded the form flow to field-level validation with per-field errors and accessible markup (`aria-invalid`, `aria-describedby`), while still preserving submitted values.
- Confirmed that the current stack can express realistic SSR validation UX without introducing a generic form layer yet.

- Added doc comments across the public crate surfaces and updated the README to describe the implemented slice rather than only the original roadmap.
- Rewrote the small `ssw-components` alert to use `html!`, keeping even optional components aligned with the same HTML authoring model.
- Review pass outcome: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` all pass; no blocking correctness or clarity issues were found beyond the work already addressed in this session.

- Added a small reusable field helper layer in `ssw-components` with borrowed field state, labeled wrappers, and input helpers for text, email, and textarea controls.
- Refactored the Actix-backed contact form to use those helpers, which removed repeated `aria-invalid`, `aria-describedby`, label, and error markup from the example flow.
- Caught and fixed a real helper bug during test-driven verification: class composition helpers must receive plain class names, not selector-like strings.
- Verification after the refactor: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` all pass again.

- Expanded `ssw-core` with flash message semantics (`FlashLevel`, `FlashMessage`) and redirect helpers that can carry transient user-facing messages across redirects.
- Added an Actix `RequestContext` that reads flash cookies, clears them after use, issues CSRF tokens, and verifies submitted form tokens against cookie-backed request state.
- Expanded `ssw-components` with `flash_notice` and `hidden_input`, then used those helpers in the Actix-backed contact and success pages.
- Updated the end-to-end Actix form flow to cover CSRF token issuance, invalid CSRF redisplay, redirect-carried flash messages, and flash clearing after the success page is rendered.
- Important current tradeoff: flash transport is still unsigned and cookie-backed, and CSRF protection is a first hook built around a double-submit-style cookie flow rather than a more complete secret/session abstraction.
- Verification after the flash and CSRF slice: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` all pass.

- Added `COMPONENTS.md` to define the `ssw-components` design direction, including the styling contract, class and slot naming conventions, token strategy, enhancement rules, and v0 component scope.
- Captured an explicit decision to treat Base UI as a philosophical reference rather than a direct implementation template, because `ssw-rs` is SSR-first and Rust-first instead of React-first.
- Captured the intended separation between semantic component structure and a future first-party default theme layer, so styling becomes a stable API before the component catalog grows.

- Applied the `COMPONENTS.md` contract to the existing `ssw-components` helpers: fields, notices, and controls now expose stable `ssw-*` classes and semantic `data-*` state hooks.
- Added the first repo-local theme stylesheet, now living at `styles/ssw-theme-default.css`, with CSS custom-property tokens and starter styles for notices, fields, inputs, and textareas.
- Important implementation note: the current `ssw-html` shorthand is not expressive enough for the desired BEM-like class names, so explicit class strings are now used where needed.
- Verification after the component contract pass: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` all pass.

- Added the first simple `ssw-components` primitives beyond form fields: `button`, `submit_button`, `container`, `section`, and `stack`.
- Expanded the first-party theme stylesheet to cover those new primitives with the same token contract, including container width, section surfaces, stack spacing, and primary/secondary button treatments.
- Kept the API deliberately small and HTML-shaped rather than introducing larger configuration objects or interactive abstractions.
- Verification after the primitive expansion: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` pass.

- Added a native `select` helper to `ssw-components` with a small `SelectOption` API, stable `ssw-select` classes, and selected-option behavior derived from the current field value.
- Added a real workspace example app at `examples/ssw-intake-demo`, with an SSR intake form that uses the current layout wrappers, field helpers, select helper, stylesheet route, flash flow, and CSRF hooks.
- The example app intentionally does not add persistence or a richer session layer yet; its role is to pressure the current primitives and request model in a real binary without hiding gaps behind more infrastructure.
- Verification after the select and example-app slice: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` pass.

- Refined the example app after a live preview pass: removed a duplicate viewport tag, removed a dead secondary action from the intake page, and added a dedicated `/style-guide` route for direct visual review of notices, buttons, fields, textarea, and select states.
- The example app now acts as both a realistic product-shaped flow and a lightweight style guide, which is a better setup for iterative API and CSS refinement than relying on source inspection alone.
- Added `scripts/capture-intake-demo.sh` so the live preview loop can produce repeatable screenshots of both the intake flow and the style guide without reassembling browser commands by hand.
- Tightened the example app's hero and page rhythm after reviewing real screenshots, reducing the amount of vertical space consumed before the useful review surface starts.
- Refined the first-party stylesheet after another live screenshot pass: section surfaces are more deliberate, notices and buttons have clearer emphasis, and form controls have stronger neutral, hover, focus, and invalid states.
- Reframed the styling split so `ssw-components` is explicitly unstyled by default and the first-party CSS moved to `styles/ssw-theme-default.css`, with the theme direction now aiming closer to `shadcn/ui` than to a baked-in component default.
- Tightened the optional theme again after comparing it against `shadcn/ui`: reduced radii and shadow weight, tightened card and control density, increased border clarity, and toned down the example page chrome so the primitives read as the main subject.
- Added an initial `ssw_html::fonts` helper for Google Fonts head integration, then wired the example app to load `Inter` remotely so the default theme can opt into a more `shadcn/ui`-like type stack without making fonts a component-layer dependency.
- Extended `ssw_html::fonts` with a self-hosted font-face helper that can emit local `@font-face` rules and preload links, giving the framework a first local-font path before any larger asset pipeline exists.
- Review pass: fixed a real safety issue in the HTML surface by making plain `&str` and `String` conversions into `Markup` escape by default instead of being treated as raw HTML. This keeps `impl Into<Markup>` helpers safer unless callers opt into `Markup::raw(...)`.
- Review pass: improved notice accessibility so error and warning flashes use `role="alert"` instead of the quieter `status` role.
- Review pass: updated public docs and the README so trusted raw HTML versus escaped text is described more explicitly.
- Split `ssw-components` into internal domain modules (`button`, `field`, `layout`, `notice`, `page`) while preserving the existing flat public API through re-exports.
- Improved `scripts/capture-intake-demo.sh` so captures can use a configurable wider viewport or full-page mode, and fixed a bug where relative output paths could fail when `agent-browser` saved screenshots.
- Added the first reusable app-shell helpers to `ssw-components`: `page_shell`, `page_header`, `page_actions`, and `card_header`.
- Refactored the intake demo to use those page-shell primitives instead of app-local hero and section-heading markup, then updated the optional theme so the visual treatment follows the component contract.
- Added the first simple app-level navigation and empty-state helpers to `ssw-components`: `top_nav`, `NavItem`, and `empty_state`.
- Added a second example app at `examples/ssw-projects-demo`, with list, detail, archive-empty-state, and edit flows to pressure the current page shell, nav, empty-state, redirect, flash, CSRF, and form primitives in a more app-shaped way.
- Tightened the optional native select theme to add a deliberate chevron gutter and right-side padding, bringing it closer to the spacing used in the current `shadcn/ui` native-select treatment.
- Extracted the first shared page-level helpers that were repeating across the demos: `link_button` for link-shaped action rows, plus `MetaItem` and `meta_list` for compact labeled metadata.
- Refactored both example apps to use those shared helpers instead of app-local link and metadata markup, which makes the examples a better pressure test for what should stay in `ssw-components`.
- Added `SSW_CSS.md`, a design note for a proposed `ssw-css` companion crate. The current recommendation is to keep scope narrow: deterministic class-based scoping for component-local CSS, plain CSS output, no client runtime, and no dependency from `ssw-components`.
- Added an initial `ssw-css` and `ssw-css-macros` crate pair. The prototype currently supports deterministic class-based scoping, string-literal selectors, raw CSS-like declaration values, raw `@media` queries, `styles.class(...)`, `styles.classes(...)`, `render()`, and `style_tag()`.
- Proved the initial `ssw-css` API in the intake style guide instead of wiring it into `ssw-components`, so the scoped-style experiment can evolve without weakening the primitive component boundary.
- Added an ergonomics pass for `ssw-css`: declaration values and media queries no longer need quoted strings, `styles.classes(...)` now joins multiple scoped slots, and `style_tag()` now marks the generated block with `data-ssw-css` for easier inspection.
- Important current caveat: CSS dimensions still have to be authored as `1 rem` or `0.08 em` because Rust tokenization does not allow `1rem` directly inside macro input. The macro joins them back into valid CSS output.
- Added a second real `ssw-css` proof point in the projects demo: project cards and status badges now use page-local scoped CSS and inject their style block into the document head, which pressures the current API against repeated app UI instead of only a style-guide sample.
- Added `SSW_WORKERS.md`, a design note for a proposed `ssw-workers` adapter. The document keeps scope narrow: Cloudflare Workers request/response integration, minimal GET/POST proof, and pressure on shared response, cookie, flash, and CSRF boundaries without moving Cloudflare product bindings into core.
- Added a first real `ssw-workers` implementation with:
  - conversion from `ssw-core::Response` into `worker::Response`
  - page, fragment, redirect, and cookie-backed request-context helpers
  - a wasm-checkable `examples/ssw-workers-demo` Worker app with GET, POST, redirect, flash, and CSRF flow
- Important architecture correction from this pass: CSRF token validation is shared, but token generation is adapter-specific. Keeping randomness in `ssw-core` created an unnecessary wasm portability problem, so token generation moved back into the adapters.
- Added a real local Workers dev workflow to `examples/ssw-workers-demo` with `wrangler.toml`, local `wrangler` package scripts, and a project-local `worker-build` install path.
- Verified the Worker example end to end under `wrangler dev`: `/`, `/theme.css`, invalid form redisplay, valid `303` redirect, flash cookie attachment, and flash clearing on `/thanks`.
- Added a small Worker-served stylesheet route so the demo is visually inspectable without inventing a broader asset pipeline yet.
- Tightened the first shared backend seam after Actix and Workers both existed: `ssw-core` now owns a backend-neutral `RequestState` for flash decoding, CSRF token reuse-or-refresh decisions, and form-token verification.
- Kept the backend boundary narrow on purpose: Actix and Workers still own request parsing, adapter-specific CSRF token generation, and response cookie application instead of forcing a broader lowest-common-denominator request abstraction into core.
- Verification after the extraction stayed clean across host and wasm targets: `cargo fmt --all`, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo doc --no-deps`, `cargo check -p ssw-workers --target wasm32-unknown-unknown`, and `cargo check -p ssw-workers-demo --target wasm32-unknown-unknown` all passed.
- Expanded the Worker demo with a real fragment route at `/preview`, driven by query string input, so the backend now proves partial HTML responses under `wrangler dev` instead of only full pages and redirects.
- Extended `ssw-core` so `HtmlResponse` and `TextResponse` now carry explicit HTTP status codes with 200 defaults, and added narrow constructors for status-bearing HTML or text without turning `Response` into a generic builder abstraction.
- Updated both adapters to honor those shared statuses when converting into `HttpResponse` and `worker::Response`, then added an Actix test to prove that non-200 HTML from core survives adapter conversion correctly.
- Replaced the Worker-native 404 helper path with `CoreResponse::html_rendered_with_status(404, ...)`, which confirms the decision cleanly: non-200 HTML now belongs in the shared response model, while cookies and request parsing still remain adapter-specific.
- Verified the updated Worker routes under local `wrangler dev`: `GET /preview?note=Shared%20status%20model` returns a text/html fragment with status 200, and `GET /missing-route` now returns a full HTML document with status 404 via the shared core response path plus the normal request-context cookie application.
- Added status-aware HTML helpers to `ssw-actix`, so non-200 document rendering no longer requires dropping to `to_http_response(...)` manually when the route still wants the normal request-context cookie handling.
- Updated the Actix test flow so invalid CSRF redisplay and invalid form redisplay now return `422 Unprocessable Entity`, and added a helper-focused Actix test to keep that status path explicit.
- Updated `examples/ssw-projects-demo` to render shell-preserving HTML 404 pages for missing projects and HTML 422 redisplay for invalid edit submissions.
- Verified the Actix example live on `127.0.0.1:3003`: `GET /projects/missing-slug` returns a full HTML page with status 404, and invalid `POST /projects/northstar/edit` returns a full HTML page with status 422 plus preserved values and field errors.
- Added a small Actix-first form helper layer in `ssw-actix/src/form.rs`: `FormData` for field access, `submitted_form(...)` and `FormSubmission::verify_csrf()` for request-context plus CSRF plumbing, and `unprocessable_page(...)` for the common HTML 422 redisplay path.
- Refactored both Actix example apps to use that helper layer instead of repeating `HashMap<String, String>` access, `request_context(...)`, and manual CSRF verification inside every POST handler.
- Useful testing note from this pass: bare `actix_web::test::TestRequest` values do not model cookie-backed raw `HttpRequest` state the same way as routed service requests, so the new form-helper tests now focus on the helper's actual contract instead of pretending the raw harness preserves request cookies.
- Verification after the form-helper pass: `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` all pass.
- Started the first asset-story slice in `ssw-html` with a new `assets` module: typed `Asset` references with optional query-string versioning, plus plain head helpers for stylesheets, scripts, preloads, and preconnects.
- Refactored the intake, projects, and Workers demos to use `ssw-html::assets` instead of handwritten stylesheet tags, and moved the public asset routes to an explicit `/assets/...` naming shape.
- Kept the serving boundary adapter-local on purpose: Actix and Workers still serve their asset routes directly, while `ssw-html` only owns markup generation and versioned asset URLs.
- Added tests for asset URL versioning and rendered head tags, including the existing-query-param case so `?v=` appending stays stable.
- Verification after the asset-helper pass: `cargo fmt --all`, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo doc --no-deps` all pass.
- Added `examples/README.md` to document the purpose, route map, and run commands for the intake, projects, and Workers demos, so new readers do not have to infer the example story from the source alone.
- Added top-level module docs to each example source file to make the API pressure and route intent visible at the file boundary.
- Updated the main repo README to point at the examples guide and to frame the examples as part of the framework story instead of disposable demos.
- Added `validation_summary` and `ValidationItem` to `ssw-components` as a small, explicit summary-level form-feedback primitive with optional links back to invalid fields.
- Refactored the intake, projects, and Workers examples to use that shared summary component for invalid submissions instead of repeating summary-level error notice assembly inside each route flow.
- Added first-party theme styling for validation-summary lists and links, and extended the intake style guide so the new primitive is visible in the visual review surface.
- Useful implementation note from this pass: `ssw-html` still does not support `@else`, and class composition inside the macro still wants a single expression such as an array, so the new component had to stay within those existing parser boundaries instead of pretending the DSL is more expressive than it is.
- Ran a broader review-and-refinement pass focused on keeping the codebase readable from first principles instead of growing incidental abstraction.
- Moved the large Actix end-to-end coverage out of `crates/ssw-actix/src/lib.rs` and into `crates/ssw-actix/tests/http_flow.rs`, keeping the runtime module materially cleaner while preserving the same public-API coverage.
- Simplified several rendering paths in `ssw-components` and the examples so optional sections are assembled before entering the `html!` macro instead of relying on repeated `is_some()` and `unwrap()` branches inside template code.
- The review pass also caught a real Worker-only lifetime bug that the host build did not surface; rerunning the `wasm32-unknown-unknown` checks after the refactor fixed it before commit.
- Ran a follow-up polish pass focused on public-surface discipline rather than larger refactors.
- Removed the unused `ssw-components::page(...)` helper because it was only a thin alias over `ssw_html::page(...).body(...).render()` and was not earning its place in the public API.
- Removed the public non-wasm `host_build_stub()` symbol from the Workers example; the crate builds fine on host targets without exposing a fake user-facing API.
- Added the first Worker-side ergonomics pass in `ssw-workers`: status-aware page helpers plus a small form helper slice with `FormData`, `submitted_form(...)`, `FormSubmission::verify_csrf()`, and `unprocessable_page(...)`.
- Refactored the Workers demo POST flow to use that helper layer instead of manual request parsing, CSRF checking, and 422 redisplay wiring.
- This keeps the backend boundary honest: the Worker ergonomics are now closer to the Actix slice, but the abstraction still lives entirely inside the adapter crate rather than leaking into `ssw-core`.
- Added a small D1-shaped documentation example in `SSW_WORKERS.md` to make the persistence boundary explicit: `ssw-workers` owns request or response ergonomics, while D1 bindings, queries, and row mapping stay in app code.
