# Session Notes

- Added `SSW_MARKDOWN.md` as the design direction for a proposed optional `ssw-markdown` crate.
- Scoped `ssw-markdown` to safe Markdown-to-`ssw_html::Markup` rendering first, with component directives, heading ids, table-of-contents support, Markdown typography, and optional `ssw-css` style collection as the central design concerns.
- Kept static site generation explicitly out of the first crate boundary; it should build later on top of Markdown rendering rather than drive the first API.
- Updated `README.md`, `ARCHITECTURE.md`, and `context/current.md` to record `ssw-markdown` as proposed design direction, not an implemented workspace member.
