# Session Notes

- Added the next SSR-native `ssw-components` slice: `badge`, `breadcrumbs`, `data_table`, and `pagination`.
- Added `stat_list` and `StatItem` as a denser metadata primitive, separate from the existing row-based `meta_list`.
- Kept `ssw-components` unstyled by default and extended `styles/ssw-theme-default.css` instead of pushing styling back into the primitive layer.
- Reused the new primitives in the intake style guide and projects demo, including breadcrumbed edit pages, paginated project list mockups, and stat-list metadata on the project detail page.
- Deliberately did not add a dedicated `card` primitive in this pass because `section` plus `card_header` still fits the current examples without introducing an overlapping abstraction.
- Verified the visual result with local screenshots from `ssw-intake-demo` and `ssw-projects-demo` after running both apps locally.
- Added `CUSTOM_COMPONENTS.md` to make the intended composition model explicit: framework-owned primitives, app-owned product components, and optional local `ssw-css`.
- Moved the projects demo card, badge, and metadata composition into `examples/ssw-projects-demo/src/components.rs` so the repo now demonstrates app-owned components as ordinary Rust functions returning `Markup`.
- Verified the new authoring path with `cargo fmt --all`, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo doc --no-deps`, and wasm checks for `ssw-workers` plus `ssw-workers-demo`.
- Added a second app-owned `ssw-css` proof point in the projects demo: `project_story_panel` plus local `copy-group`, `copy`, and `form` styles now own the detail, edit, and 404 narrative surfaces instead of keeping that styling in page-global CSS.
- The follow-up ergonomics signal from this pass is that the current primitive and `ssw-css` layers were sufficient; the remaining visible verbosity is mostly local `Field::new(...)` setup in the edit flow, not an obvious missing framework abstraction yet.
- Collapsed the remaining edit-form field-binding noise with an app-owned `EditField::required_field(...)` helper in `ssw-projects-demo`, which kept the improvement local and confirmed that repeated `Field` setup still does not justify a new framework API.
